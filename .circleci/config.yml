version: 2
jobs:
  build:
    docker:
      - image: elixir:1.13.1
    steps:
      - checkout
      - run: mix local.hex --force
      - run: mix local.rebar --force
      - run: mix deps.get
      - run: mix test
      - run: mix credo
